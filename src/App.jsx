import Post from "./components/Post";

function App() {
  return <Post />; // A function pointer isn't it?
  // The custom components must start with an upper case character
  // Default components start with a lower case character
}

export default App;
